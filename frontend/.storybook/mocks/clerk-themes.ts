/**
 * Storybook stand-in for `@clerk/ui/themes` (aliased in `.storybook/main.ts`).
 * The mock UserButton ignores `appearance`, so empty theme objects suffice.
 */
export const dark = {};
export const shadcn = {};
